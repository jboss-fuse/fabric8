This command can be used to retrieve the ensemble password.

To change this password, you can use
fabric:ensemble-password newpassword
to reset the password and then use
fabric:ensemble-password --commit 
to save the new password

